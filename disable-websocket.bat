@echo off
echo Disabling WebSocket connections to prevent console errors...
echo.

echo This will prevent WebSocket connection attempts that cause console errors.
echo The app will work without real-time features.
echo.

echo To re-enable WebSocket later, you can call webSocketService.enable() in your code.
echo.

pause
